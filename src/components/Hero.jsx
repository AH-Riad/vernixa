import React from "react";

const Hero = () => {
  return (
    <div className="h-screen w-full bg-[url('/gradientBackground.png')] bg-cover bg-center">
      Hero
    </div>
  );
};

export default Hero;
